<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h2>Thank you for your order, <%@order.name%></h2>
    <p>
        You have ordered the following items: </p>
      <div class='order-summary'>
            <h3>Order Summary</h3>
            <table class='table'>
            <% @order.line_items.each do |t|%>
              <tr>
               <td><%=t.product.productName%></td>
                <td><%=t.quantity%></td>
                 <td><%=number_to_currency(t.total_price)%></td>
                </tr>
                <%end%>
                 <tr class="total_line">
        <td colspan='2'>Total</td>
        <td class="total_cell"><%=number_to_currency(@order.order_total) %></td>
    
    </tr>
            </table>
        </div>
    
    <p>We appreciate your business!</p>
  </body>
</html>